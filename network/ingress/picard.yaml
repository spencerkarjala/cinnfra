apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: picard
  namespace: apps
spec:
  ingressClassName: traefik
  rules:
  - host: picard.skarja.la
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: picard
            port:
              number: 80