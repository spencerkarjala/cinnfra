apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: reference-compiler
  namespace: apps
spec:
  ingressClassName: traefik
  rules:
  - host: reference-compiler.skarja.la
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: reference-compiler
            port:
              number: 80
