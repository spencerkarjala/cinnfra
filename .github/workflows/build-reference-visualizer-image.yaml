name: Build and push image for reference-visualizer

on:
  push:
    paths:
      - 'apps/reference-visualizer/**'
      - '.github/workflows/build-reference-visualizer-image.yaml'
  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: build-reference-visualizer-image
  cancel-in-progress: true

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository_owner }}/reference-visualizer

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log into GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: apps/reference-visualizer/dockerfile
          push: ${{ github.event_name != 'pull_request' }}
          tags: |
            ghcr.io/${{ github.repository_owner }}/reference-visualizer:latest
